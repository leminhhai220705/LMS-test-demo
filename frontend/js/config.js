// // Frontend config: API base URL.
// // In production, set window.APP_API_BASE_URL before this script (e.g. via hosting env).
// // Fallback: empty string = same origin as frontend.

// window.APP_API_BASE_URL = window.APP_API_BASE_URL || "";

window.APP_API_BASE_URL = "https://lms-backend-hf26.onrender.com";
// window.APP_API_BASE_URL = "";
